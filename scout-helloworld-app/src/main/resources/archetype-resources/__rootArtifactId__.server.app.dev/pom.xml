<?xml version="1.0" encoding="UTF-8"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd" xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">

  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>${groupId}</groupId>
    <artifactId>${groupId}.parent</artifactId>
    <version>${version}</version>
    <relativePath>../${groupId}.parent/pom.xml</relativePath>
  </parent>

  <artifactId>${groupId}.server.app.dev</artifactId>
  <packaging>war</packaging>

  <name>${displayName} HTML UI WAR Development</name>

  <dependencies>
    <dependency>
      <groupId>org.eclipse.scout.rt</groupId>
      <artifactId>org.eclipse.scout.dev.jetty</artifactId>
    </dependency>
    <dependency>
      <groupId>${groupId}</groupId>
      <artifactId>${groupId}.server</artifactId>
    </dependency>
  </dependencies>
</project>
